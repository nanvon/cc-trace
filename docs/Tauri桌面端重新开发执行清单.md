# 全新 Tauri 跨端桌面应用执行清单

> 目标：在独立新仓库中，使用 Tauri 2、Vue 3、TypeScript 和 Rust，从零实现一个 macOS / Windows 桌面应用。
>
> 新应用不是当前 Swift 版 cc-bar 的重构分支或原地升级，不迁移、不读取、不删除旧 cc-bar 应用数据。当前 cc-bar 只作为业务事实、Fixture 和问题经验的参考来源。
>
> 本文写于产品名称确认之前，因此正文沿用“新应用”这个说法；它指的就是 CC Trace。

## 阶段状态总表

逐项勾选见后续各节；本表是唯一的顶层进度视图。

| 阶段 | 状态 | 阻塞点 |
|---|---|---|
| 1 确认项目身份 | 已完成 | — |
| 2 定义新产品 | 已完成 | — |
| 3 锁定首版范围 | 已完成 | — |
| 4 固化数据边界 | 已完成 | — |
| 5 整理可参考的旧版事实 | 基本完成 | Codex、Claude Code 脱敏 Fixture 已加入；协议真机复核待完成 |
| 6 重做信息架构 | 已确认 | 主窗口按 [ADR-0020](决策/ADR-0020-主窗口改为本地用量页.md) 改为本地用量页并有评审草图；额度历史（Timeline）与 Conversations 已作为主窗口第二、三视图进入首版（[ADR-0020](决策/ADR-0020-主窗口改为本地用量页.md) 第 8 节、[信息架构与核心流程](信息架构与核心流程.md) §6.4～6.5）；统一导航形态已由 [ADR-0024](决策/ADR-0024-主窗口分组侧边栏导航.md) 定为分组侧边栏并落地，视觉修复取值已由 [ADR-0023](决策/ADR-0023-视觉修复方向定稿.md) 定稿 |
| 7 确定设计方向 | 已完成 | — |
| 8 完成交互原型 | 已完成；同窗路径已由 macOS 日常使用验证 | 独立设置窗口结论已被 ADR-0011 取代 |
| 9 确定技术架构 | 已完成 | — |
| 10 创建独立新仓库 | 已完成 | — |
| 11 验证双平台桌面壳 | macOS 已完成（日常使用验证） | 2026-08-10 产品所有者确认以日常使用为实机证据（macOS 27）：Tray、窗口、锚点、去抖、快捷键、设置持久化、首次启动、开机启动均已在真实使用中工作；剩余专项缺口见下方「已知缺口与不阻塞声明」；**Windows 从未实机运行** |
| 12 打通 Provider 最小闭环 | macOS 已完成（日常使用验证） | 按 [ADR-0015](决策/ADR-0015-第12阶段先于桌面壳实机验证启动.md) 与第 11 阶段并行启动。2026-07-27 首次真实数据验证后，手动刷新、缓存、token 回写共存已由日常使用覆盖（2026-08-10 确认）；异常态（429、拒绝授权、失败快照保留）日常未触发，保持未验证；**Windows 侧全部未验证** |
| 13 实现首版功能 | 开发已完成，macOS 日常使用验证 | 全部功能切片已落地并通过自动化检查（Rust 242 项、vitest 110 项、vue-tsc／eslint／prettier），产品所有者已实际使用；浏览器与正式走查未做，**Windows 未验证** |
| 14 完成首次初始化 | 已完成（macOS 日常使用验证） | 独立数据目录、默认设置、缓存、schemaVersion 与首次全量索引均在 2026-08 的真实使用中实际发生；损坏恢复、GB 级压力与隔离审计未验证 |
| 15 业务结果验证 | 基本完成（macOS） | 脱敏 Fixture、真实数据对照（2026-08-08 记录）与日常真实使用已验证；异常态与 Windows 未验证 |
| 16 双平台验收 | macOS 日常使用覆盖，正式验收未做 | 2026-08-10 产品所有者确认 macOS 侧以日常使用为验收证据；正式走查（安装／卸载、英文、深色、无障碍）与 **Windows 验收**未做 |
| 17 独立发布 | 基础设施已实跑 | `v0.1.12` tag 触发 Release workflow 成功（2026-08-08），5 个分发产物＋`SHA256SUMS.txt`，Release 保持 Draft；产物真机验证与正式公开未做 |

## 已知缺口与不阻塞声明（2026-08-10）

首版功能已开发完成并交付 macOS 日常使用；产品所有者于 2026-08-10 确认**以日常使用
作为 macOS 实机验证证据**（macOS 27）。以下缺口**不阻塞**文档收尾与 macOS 侧继续使用，
但每一项都**不得**被写成已验证：

| 缺口 | 状态 |
|---|---|
| Windows 全部实机行为（Tray、凭据发现、透明窗口、路径、面板高度等） | 无 Windows 主机，全部未验证 |
| macOS 26 及以下旧路径回归 | 未验证（日常仅在 macOS 27） |
| 正式安装与卸载（DMG 安装、卸载残留） | 未验证 |
| 英文界面、深色模式专项走查 | 未验证（日常中文＋浅色） |
| 损坏恢复路径（删除／写坏 `settings.json`、缓存损坏、`usage.db` 损坏） | 未验证（日常未触发） |
| 异常状态（钥匙串拒绝授权、429 退避、网络失败快照保留） | 未验证（日常未触发） |
| 审计类（新旧应用并行、目录隔离、Vue 层不能取得秘密） | 未验证 |
| 真实 GB 级全量索引压力 | 未验证（首次索引已在真实使用中实际完成） |
| Release 产物真机验证（Gatekeeper、升级后钥匙串授权、SmartScreen） | 未验证（`v0.1.12` Release 为 Draft） |
| 正式公开（Publish Release、1.0.0） | 未做 |

阶段不是严格串行：第 5 阶段与第 6～11 阶段可以并行，但它是第 12、15 阶段的前置输入。第 5 阶段的事实产物是 [额度领域模型](额度领域模型.md)。

第 17 阶段的 GitHub 自动打包基础设施可提前建立，只能创建 Draft Release，不代表
macOS 侧已通过正式验收，也不授权在双平台实机验收前公开发布。

## 文档放置与路径约定

将本文件和方案文档复制到新仓库的 `docs/` 目录。推荐让新仓库与当前 `cc-bar` 仓库位于同一父目录。

本文所有路径都以**新仓库根目录**为基准：

| 路径 | 含义 |
|---|---|
| `.` | 新应用仓库根目录 |
| `docs/` | 新应用自己的文档 |
| `../cc-bar` | 当前 Swift 版 cc-bar 源码仓库，只读参考 |

旧版应用入口为 `../cc-bar/CCBarApp.swift`，核心业务代码位于 `../cc-bar/Core/`。文档不记录个人电脑的绝对路径。

如果新仓库最终没有与 `cc-bar` 放在同一父目录，复制后应统一调整 `../cc-bar`。新应用的源码、构建、测试和 CI 不得依赖旧仓库存在。

## 1. 确认项目身份

- [x] 确认新仓库名称。
- [x] 确认新仓库与旧仓库的相对位置。
- [x] 确认旧版源码可通过 `../cc-bar` 只读访问；若路径不同，统一更新本文约定。
- [x] 确认正式产品名称。
- [x] 确认是否继续使用 CCBar 品牌。
- [x] 明确新应用不是 Swift 版的无损升级。
- [x] 确认新 Bundle Identifier。
- [x] 确认 Windows Application ID。
- [x] 确认新应用数据目录和配置 namespace。
- [x] 确认新的 Keychain / Credential Manager service namespace。
- [x] 确认新的开机启动注册标识。
- [x] 确认开发阶段新旧应用可以并行运行。

## 2. 定义新产品

- [x] 写清一句话产品定位。
- [x] 确认目标用户。
- [x] 确认用户最核心的三个任务。
- [x] 明确新应用与当前 cc-bar 的关系。
- [x] 明确首版非目标。
- [x] 明确不默认继承旧版功能清单。
- [x] 明确不复刻旧版页面和视觉。
- [x] 建立独立的 `产品定义.md`。

## 3. 锁定首版范围

- [x] 将候选能力分为“首版必须 / 首版简化 / 后续版本 / 明确不做”。
- [x] 确认 macOS Menu Bar 和 Windows System Tray。
- [x] 确认 Codex、Claude Code 的首版额度范围。
- [x] 确认首版刷新、缓存、错误和设置范围。
- [x] 确认首版是否包含本地用量汇总。
- [x] 确认首版是否包含 Conversations。
- [x] 确认首版是否包含 Timeline 或额度变化。
- [x] 确认首版是否包含 Pricing 和费用计算。
- [x] 确认首版是否包含其他 Codex 账号。
- [x] 确认首版是否包含桌面悬浮窗。
- [x] 确认首版语言和外观范围。
- [x] 为每项首版能力写出数据源和验收标准。
- [x] 建立独立的 `产品范围.md`。

## 4. 固化数据边界

- [x] 明确旧 cc-bar 应用数据全部不读取、不迁移、不修改。
- [x] 明确不读取旧 UserDefaults。
- [x] 明确不读取旧额度缓存和额度历史。
- [x] 明确不读取旧 Usage / Conversation Rollup。
- [x] 明确不读取旧 Scan State。
- [x] 明确不导入旧版保存的其他账号。
- [x] 明确不读取、覆盖或删除旧版专用安全存储项。（本条只约束 Swift 版 cc-bar 的存储项，不受 [ADR-0014](决策/ADR-0014-token刷新结果回写外部凭据.md) 影响。）
- [x] 明确不继承旧悬浮窗位置和 Onboarding 标记。
- [x] 明确 Codex / Claude Code 凭据属于外部数据源。
- [x] 明确 Codex / Claude Code 原始日志属于外部数据源。
- [x] 明确外部日志只读。（凭据的 token 刷新回写例外见 [ADR-0014](决策/ADR-0014-token刷新结果回写外部凭据.md)；外部日志本身仍然只读。）
- [x] 定义新应用自己的数据目录、缓存和安全存储。
- [x] 定义新应用 `schemaVersion`。
- [x] 定义原子写入、损坏恢复和可重建数据策略。
- [x] 记录新应用未来版本之间的升级入口。

## 5. 整理可参考的旧版事实

本阶段的事实产物是 [额度领域模型](额度领域模型.md)；提取自 [cc-bar 只读参考](cc-bar-reference/README.md) 的静态审计结果，协议细节仍需在第 12 阶段用真实凭据复核。

- [x] 从 `../cc-bar/Core/Quota/` 整理 Provider 请求和字段语义。（见额度领域模型第 1～3 节）
- [x] 从 `../cc-bar/Core/Credentials/` 和 `../cc-bar/Core/Quota/` 整理凭据发现和 token 续期规则。（见第 5 节）
- [x] 从 `../cc-bar/Core/Scheduler.swift` 和 `../cc-bar/Core/Quota/` 整理刷新、节流和 429 退避规则。（见技术架构“首版参数基线”与状态与错误模型第 5 节）
- [x] 从 `../cc-bar/Core/Quota/` 和 `../cc-bar/Core/AppState.swift` 整理错误分类和快照保留原则。（见状态与错误模型第 4 节、额度领域模型第 4 节）
- [x] 若首版包含用量，整理 JSONL 路径、格式、截断、覆盖、半行和去重规则。（2026-07-30 已结合旧版参考与本机只读结构抽样，现行结论见 [数据存储与用量索引](数据存储与用量索引.md)）
- [x] 若首版包含费用，整理 Pricing、Fast／Priority、缓存 TTL、地域与长上下文口径。（2026-07-30 已按官方价格来源固化公开目录与整数费用算法）
- [x] 从 `../cc-bar/CCBarTests/` 提取必要的脱敏 Fixture 和测试预期。（已按已确认
  字段人工构造并分别落入 `fixtures/providers/codex/` 与 `fixtures/providers/claude/`，
  见 `fixtures/README.md`；均不是真实响应副本）
- [x] 从 `../cc-bar/docs/README.md` 进入旧版产品、技术、设计和界面文档。
- [x] 记录每项参考内容是“业务事实 / Fixture / 平台经验”中的哪一种。（cc-bar-reference 使用证据等级标注）
- [x] 不复制旧 Swift 模块结构作为新架构。
- [x] 不把旧版所有行为自动列为新应用验收要求。

## 6. 重做信息架构

- [x] 按 `AGENTS.md` 使用 `impeccable`；边界仍不明确时先使用 `grilling`。
- [x] 确定紧凑额度入口承担的内容和操作。
- [x] 确定主窗口职责。
- [x] 确定设置和首次启动职责。
- [ ] 若包含本地用量，确定其导航和渐进呈现方式。（紧凑入口的今日／本周费用与扫描呈现已确认；
  主窗口用量页的结构已由 [ADR-0020](决策/ADR-0020-主窗口改为本地用量页.md) 确认，额度历史
  Timeline 已作为第二视图落地（第 8 节）；Conversations、取消与分页仍待后续 UI 切片）
- [x] 若包含悬浮窗，确定它与主窗口的关系。（首版不包含，不预留入口。）
- [x] 确定主窗口是本地用量页而非额度总览，并完成评审草图。
      （[ADR-0020](决策/ADR-0020-主窗口改为本地用量页.md)、`prototypes/visual-refresh/`，2026-07-30。
      草图为静态 HTML，不连数据、不含交互，不构成任何实现或平台验证证据。）
- [x] 确定额度异常「影响 / 下一步」的落脚点。
      （已解决：主窗口不再显示额度后，由紧凑面板的异常 Provider lane 可展开承载三级详情
      「标题／影响／下一步」，见 ADR-0020「未解决问题」。2026-08-08 落地。）
- [x] 确定缓存命中率的分母是否含缓存写入。
      （采用标准化 `input_tokens`，含缓存写入；见 [数据存储与用量索引](数据存储与用量索引.md) §2.1.1，
      已在主窗口 Provider 卡与模型表统一实现。）
- [x] 完成核心用户流程图。
- [x] 完成页面和窗口关系图。
- [x] 确认旧页面名称、Tab 和卡片结构不约束新版。

## 7. 确定设计方向

> 视觉语言经三次改向：[ADR-0012](决策/ADR-0012-视觉方向改为HeroUI风格.md) 由 Reset Rail
> 改为 HeroUI 风格；[ADR-0017](决策/ADR-0017-系统区域显示额度数字与余量分档.md) 进一步
> 把紧凑面板与主窗口的**布局**对齐 HeroUI 探索稿，并新增余量四档色；[ADR-0028](决策/ADR-0028-全部表面改为贴合式弱化卡片层级.md)
> 把层级机制改为贴合式弱化卡片（删静止阴影、hairline 描边、KPI 总览行、侧边栏蓝底白字、
> 字号 scale 收敛）。信息架构与状态语义在历次改向中都没有变。

- [x] 按 `AGENTS.md` 使用 `frontend-design`、`apple-design`、`impeccable`，进入微交互细节时使用 `make-interfaces-feel-better`。
- [x] 确定整体视觉气质。
- [x] 确定字体、颜色、间距、圆角和状态色。
- [x] 确定浅色、深色和跟随系统规则。
- [x] 确定基础组件和额度展示组件。
- [x] 确定 Loading、Empty、Error、Stale、Offline 状态。
- [x] 确定窗口、弹层和系统区域的视觉关系。
- [x] 确定动效、反馈和 reduced motion 原则。
- [x] 确定键盘、焦点和无障碍原则。
- [x] 验证中英文、长账号名和长模型名。

## 8. 完成交互原型

- [x] 按 `AGENTS.md` 使用 `impeccable` 和 `web-design-guidelines`，视觉完成后使用 `kill-ai-slop` 做针对性检查。
- [x] 完成 macOS 菜单栏入口原型。
- [x] 完成 Windows 托盘入口原型。
- [x] 完成紧凑额度入口原型。
- [x] 完成主窗口原型。
- [x] 完成设置原型。
- [x] 完成首次启动原型。
- [x] 按产品范围完成可选模块原型。（首版无可选模块，不创建占位导航。）
- [x] 走通加载、成功、空数据、错误、旧快照和刷新状态。
- [x] 走通键盘和焦点路径。
- [x] 确认原型后再进入正式页面实现。

## 9. 确定技术架构

- [x] 划分 Vue、Rust、Tauri 和平台适配层。
- [x] 定义首版 commands、events 和 contracts。
- [x] 确定 Pinia、Vue Router、主题和国际化方案。
- [x] 确定 Provider 抽象。
- [x] 确定调度、刷新、节流和退避结构。
- [x] 确定新版本地存储和安全存储结构。
- [x] 确定 macOS / Windows 平台差异边界。
- [x] 确定 Tauri capabilities 最小权限。
- [x] 确定首版单应用结构。
- [x] 只有出现真实复用需求时才拆分 packages / crates。
- [x] 建立独立的 `技术架构.md`。

## 10. 创建独立新仓库

- [x] 创建新 Git 仓库。
- [x] 初始化 README、LICENSE 和基础文档。（License 由产品所有者选定为 MIT。）
- [x] 创建 Tauri 2 + Vue 3 + TypeScript + Vite 工程。
- [x] 配置 pnpm 和 Cargo。
- [x] 配置 Pinia、Vue Router、主题和国际化。
- [x] 创建 Rust 业务模块和平台适配目录。
- [x] 配置 macOS、Windows 开发入口。
- [x] 配置独立应用标识和数据目录。
- [x] 确认新仓库不包含 Swift / Xcode 工程。
- [x] 确认新仓库不依赖旧仓库才能构建。
- [x] 确认 CI 中不存在对 `../cc-bar` 的运行时或构建依赖。
- [x] 建立新应用自己的 CI 基线。（`.github/workflows/ci.yml`，macOS 与 Windows 两个 job，含前端 lint／build／test 与 Rust fmt／clippy／test。）

## 11. 验证双平台桌面壳

既有实现与自动化检查曾完成；[ADR-0011](决策/ADR-0011-设置并入主窗口.md) 确认设置改为主窗口内二级视图，窗口拓扑现已收敛。2026-08-10 产品所有者确认以 macOS 日常使用为实机验证证据（macOS 27），逐项勾选见下方；剩余专项与 Windows 缺口见「已知缺口与不阻塞声明」。

- [x] 在 macOS 显示 Tray / Menu Bar 图标。（macOS 27 日常使用验证，2026-08-10
      产品所有者确认；[ADR-0017](决策/ADR-0017-系统区域显示额度数字与余量分档.md) 的
      「标识 + 百分比」模板位图在真实菜单栏中可辨识、随刷新更新）
- [ ] 在 Windows 显示 System Tray 图标。（已知缺口：无 Windows 主机）
- [x] 使用同一份合成数据展示紧凑入口。
- [x] 使用同一套 Vue 页面打开主窗口。
- [x] 将设置并入 `main` 窗口：同窗进入、返回额度、焦点恢复，不保留独立 `settings` 窗口。（日常使用验证：设置同窗往返正常，2026-08-10 确认）
- [x] 创建首次启动窗口与完成标记持久化。
- [x] 用三维状态契约替换预览期枚举，覆盖 9 种状态组合。
- [x] 实现请求合并、手动刷新节流、429 与网络退避（含单元测试）。
- [x] 验证窗口打开、重新打开和退出。（日常使用验证：关闭即隐藏、重复打开只聚焦、退出后进程与 Tray 驻留）
- [x] 验证系统安全存储。（日常使用验证：Claude Code 钥匙串一次授权、不再反复弹窗，读写正常；存储内容审计见「已知缺口」）
- [x] 验证开机启动。（日常使用验证：设置开关打开后重启生效，2026-08-10 产品所有者确认）
- [x] 验证应用数据目录。（日常使用验证：设置、缓存、用量库持续正常读写；目录隔离审计见「已知缺口」）
- [ ] 验证新旧应用可以并行运行。（已知缺口：日常未并行运行旧版 cc-bar）
- [ ] 验证新应用不访问旧 cc-bar 应用数据目录。（已知缺口：审计类）
- [ ] 验证 Vue 层不能取得秘密。（已知缺口：审计类，capability 最小权限已在架构上保证）

## 12. 打通 Provider 最小闭环

> 2026-07-26 用户明确允许暂时越过执行顺序，先后实现 Codex、Claude Code 脱敏
> Fixture 与离线响应解析。
>
> 2026-07-27 本阶段按 [ADR-0015](决策/ADR-0015-第12阶段先于桌面壳实机验证启动.md)
> 正式启动，与第 11 阶段并行。同时确认两项推翻既有结论的决策：
> [ADR-0013](决策/ADR-0013-macOS读取ClaudeCode钥匙串凭据.md) 在 macOS 增加 Claude Code
> 钥匙串凭据来源，[ADR-0014](决策/ADR-0014-token刷新结果回写外部凭据.md) 允许 token
> 刷新结果回写外部凭据来源——后者局部推翻第 4 阶段固化的数据边界，第 4 阶段
> 「明确外部日志只读」等其余条目不受影响。

> 进度（2026-07-27）：代码与自动化检查全部完成（`cargo fmt`、`cargo clippy` 的 debug 与
> release、`cargo test` 117 项、`pnpm lint`、`pnpm test` 26 项、`pnpm build`，均在 macOS
> 主机通过）。**同日在 macOS 完成第一次真实数据验证**，两个 Provider 的额度闭环跑通，
> 结果见 [桌面壳验证记录](桌面壳验证记录.md)「实机发现」。下方只勾选有实机证据的条目，
> 其余保持未勾；**Windows 侧全部未验证**。

- [x] 只读发现一个 Provider 的外部凭据。（macOS，2026-07-27：Codex 读 `auth.json`，
  Claude Code 读 macOS 钥匙串；PAT 判 `unsupported` 只有 Fixture 覆盖）
- [x] 在 Rust 层获取并标准化额度数据。（macOS，2026-07-27：两个 Provider 的真实响应
  均正确解析，窗口类型全部命中，无 `unknown`）
- [x] 通过 command 返回额度快照与展示身份。（macOS，2026-07-27：账号显示为
  `n***@gmail.com`，载荷不含完整邮箱与 token；2026-08-09 按
  [ADR-0025](决策/ADR-0025-非隐私模式显示完整邮箱.md) 改为非隐私模式显示完整邮箱，
  载荷含完整账号、仍不含 token，实机复核待补）
- [x] 在紧凑入口展示额度。（macOS，2026-07-27）
- [x] 在主窗口展示额度。（macOS，2026-07-27）
- [x] 支持手动刷新。（日常使用验证：用量页手动刷新与节流生效，2026-08-10 产品所有者确认）
- [ ] 支持 `loading`、`live`、`stale`、`no_credentials`、`offline`、`rate_limited` 和 `error`。
  （HTTP 与凭据失败已按状态与错误模型第 4 节分类，单元测试覆盖；日常使用主要呈现 `live`，
  异常态未在真实使用中触发，保持未验证——已知缺口）
- [x] 写入并读取新应用缓存。（日常使用验证：`quota-cache.json` 持续读写、额度跨重启保留；
  损坏即删除重取的恢复路径未验证——已知缺口）
- [ ] Provider 失败时保留新应用已有快照。（调度层已保证，单元测试覆盖；日常未触发失败场景，保持未验证——已知缺口）
- [x] 在 macOS 验证相同闭环。（日常使用验证，2026-08-10 产品所有者确认）
- [ ] 在 Windows 验证相同闭环。（**没有 Windows 主机，无法完成**——已知缺口）

实机验证时必须一并复核：

- [x] 钥匙串首次读取的授权弹窗表现。（macOS，2026-07-27 发现每次重新编译都弹；2026-07-28
  以 `scripts/dev-sign.sh` 固定签名标识解决，日常使用确认一次授权、不再反复弹）
- [ ] 拒绝钥匙串授权时是否正确落 `error` 而不是 `no_credentials`。（已知缺口：日常未拒绝授权）
- [x] CC Trace 刷新并回写后，Codex CLI 与 Claude Code CLI 仍能正常使用。（日常使用验证：
  应用运行期间 CLI 一直正常，2026-08-10 产品所有者确认）
- [x] Codex CLI 或 Claude Code 刷新后，CC Trace 能读到新 token 并继续工作。（日常使用验证：
  两侧长期共存、额度持续正确）
- [ ] 真实响应字段与 `docs/额度领域模型.md` 第 6 节「待复核清单」逐项对照。（未做静态逐项对照，保持待复核）

## 13. 实现首版功能

> 2026-07-30 产品所有者明确授权先完成本地用量 Rust 数据切片。该切片已完成 command
> 契约、SQLite、扫描、价格、恢复、Fixture 与定向测试；这是本阶段的窄范围例外，不代表
> “每个切片同时完成 Vue／双平台验收”的通用门槛被撤销。
>
> 2026-08-01 产品所有者确认开始还原主窗口用量页。本轮已补齐 Vue 页面、日期筛选、Provider
> 卡、ECharts 日成本图、按模型排序表、i18n、加载／空／失败态与主窗口额度读取隔离；尚未运行
> 自动化、浏览器或实机验证。

- [x] 按 `产品范围.md` 的优先级纵向实现。（首版功能已全部落地并交付 macOS 日常使用，2026-08-10 确认）
- [x] 每个切片同时完成 Rust 核心。
- [x] 每个切片同时完成 Tauri command / event。
- [x] 每个切片同时完成 Vue 页面和交互。
- [x] 每个切片同时完成状态、错误和空数据。（开发态完成；异常态的实机表现见「已知缺口」）
- [x] 每个切片同时完成必要测试。（Rust 242 项、全量 vitest 110 项）
- [ ] 每个切片同时完成 macOS、Windows 验收。（macOS 侧由日常使用验证；**Windows 全部未验证**——已知缺口）
- [x] 不按旧 Swift 模块顺序机械翻译。
- [x] 不顺手加入尚未进入首版范围的旧功能。
- [x] 换掉紧凑面板的设置图标：已改为带完整齿牙外轮廓和中心孔的齿轮，不使用容易撞形为太阳／亮度切换的放射线画法，见[设计方向与状态规范](设计方向与状态规范.md)第 7.5 节。
- [x] 完成本地用量 Rust 核心：增量扫描、半行、截断／覆盖、去重、六维 Token、SQLite 分域、价格目录、API 等值费用、聚合查询、迁移与恢复。
- [x] 同步 cc-bar 在线价格与 Fast 逻辑：LiteLLM／models.dev Standard + Fast、ETag、
  24 小时常规刷新、缺价 30 分钟冷却、手动更新、active／pending 安全边界、SQLite v2
  重计价、Claude 缺 speed／Cache Write 修正、Codex Cache Write 兼容、长上下文与历史特殊
  价格、Fast 计费等效 Token 与倍率；已补测试代码和文档，2026-07-30 本地 Rust library
  208 项与 Vue 73 项测试通过；未做真实联网或桌面实机验证。
- [x] 完成本地用量 Tauri command 与脱敏查询契约；不创建 Vue 占位页面或空导航。
- [x] 落地主窗口用量页首版 Vue 实现：八档日期筛选、自定义日期、总 Token／花费、Provider
  卡、每日成本图、按模型分组表与排序；缓存命中率统一采用含缓存写入的标准化 `input_tokens`
  分母。自动化、浏览器与实机验证待后续门禁。
- [x] 完成紧凑面板各 Provider 今日／本周 API 等值费用的前端与测试代码；排版对齐 ccbar，
  第二行只显示“花费 / Cost”且不显示状态说明或金额 `+`，不增加手动扫描按钮，首次立即扫描、
  之后固定每 5 分钟增量扫描，并在首次读取与扫描期间显示灰色 loading；已修复扫描在两次
  前端轮询之间完成后汇总不更新的竞态。部分已定价时显示可计算金额，不显示未定价说明；
  本轮未运行自动化或实机验证，主窗口仍不预留入口。
- [x] 真实 Provider 成功刷新时按身份指纹记录额度整数变化；debug 合成场景不写历史。
- [x] 落地额度历史（Timeline）视图：新增 `usage_get_quota_history` command 与 `QuotaHistory`
      DTO；每个 Provider 展示活动序列（最新事件点所属身份与窗口）的曲线＋表格、当前剩余与
      今日变化；主账号镜像去重到序列级。范围与边界见
      [ADR-0020](决策/ADR-0020-主窗口改为本地用量页.md) 第 8 节。自动化通过，浏览器与实机验证待门禁。
- [x] 落地 Conversations 列表与详情视图：`UsageConversationQuery` 增加 `project` 筛选与
      `sort`（Recent／Tokens／Cost），新增 `usage_get_conversation_breakdown`（模型／速度拆分）；
      列表支持搜索、Provider 筛选、排序与分页，详情展示全生命周期、Token 构成、费用、速度档位
      与按模型明细。参考 cc-bar F-17，自动化通过，浏览器与实机验证待门禁。
- [x] 接入 Pi 用量数据源：schema 升至 v3（`usage_entries`／`conversations`／`scan_files`
      `source` CHECK 含 `'pi'`，`quota_events` 不变）；增量扫描 `~/.pi/agent/sessions/`，
      仅 assistant 消息与根级 compaction 计入，会话自带 cost 不走价格表，entry id@时间戳全局
      去重（由 `(source, dedup_key)` 唯一索引承担）；已补脱敏 Fixture 与解析／扫描测试。
      见 [Pi数据源](Pi数据源.md) §8，自动化通过，浏览器与实机验证待门禁。
- [x] 接入 OpenCode 用量数据源：schema 升至 v4（source CHECK 含 `'opencode'`，新增
      `opencode_state` 表存 `watermark_ms`＋`seen_ids`）；只读扫描
      `~/.local/share/opencode/opencode.db`，官方 `cost` 为费用总额真值、不走价格表，
      reasoning 已含在 output，零费用跳过，模型缺失写 NULL；已补 SQLite fixture 与扫描测试。
      见 [OpenCode数据源](OpenCode数据源.md) §8，自动化通过，浏览器与实机验证待门禁。
- [x] 落地统计服务过滤：设置页「统计服务」组（Codex／Claude Code／Pi／OpenCode 默认全开，
      写入 `settings.json`）；用量页 KPI 与 Provider 卡、每日柱图、按模型表、对话列表按可见
      服务统一过滤，总量与占比分母只计可见服务（归一化），全关时显示空态引导；用量页与
      对话列表同步新增 Pi／OpenCode 四源展示。参考 cc-bar F-28，自动化通过，浏览器与实机验证待门禁。
- [x] 落地隐私模式：设置页开关写入 `settings.json`；紧凑入口（ProviderLane）在开启时隐藏
      账号标识，仅 UI 隐藏、不承诺隐私隔离（cc-bar F-24 边界）。自动化通过，浏览器与实机验证待门禁。
- [x] 落地官方服务状态（[ADR-0026](决策/ADR-0026-Statuspage状态链进入首版.md)）：OpenAI／
      Anthropic 公开 Statuspage 状态链，5 分钟独立循环＋启动立即拉取＋全量手动刷新附带，
      失败保留旧值；紧凑面板 ProviderLane 身份行右侧 6px 圆点（`unknown` 不画、`maintenance`
      新增 `--status-maintenance` token），tooltip 显示说明与更新时间；设置页「通用」组新增
      开关（默认开，只控制绘制）；独立契约与事件 `service-status://updated`，不进额度三维
      状态。Rust 与前端测试已补，2026-08-09 自动化通过，浏览器与实机验证待门禁。
- [x] 落地本地用量数据重建（[ADR-0027](决策/ADR-0027-本地用量数据重建进入首版.md)）：
      `usage_rebuild_data` command（可选 `sources`，缺省全量）删除用量条目、无引用对话与
      扫描水位后立即全量重扫；入库冲突改为 Pi／OpenCode 官方直存 cost 无条件覆盖、
      Codex／Claude 保持只增不减；设置页「用量与计价」组「重新计算用量」按钮（二次确认、
      危险色、10 秒自动退回、轮询扫描状态给完成反馈）。Rust 233 项与全量 vitest 103 项通过，
      浏览器与实机验证待门禁。
- [x] 按 [ADR-0023](决策/ADR-0023-视觉修复方向定稿.md)／[ADR-0024](决策/ADR-0024-主窗口分组侧边栏导航.md)
      定稿并落地主窗口重构（2026-08-08）：视觉修复取值（canvas 加深、阴影加档、大读数 26px、
      ok 档 success 绿）与分组侧边栏导航（视图组＋数据源组＋设置钉底，176px）写入
      `tokens.css`（已在前轮落地）与 `MainSidebar.vue`／`MainWindowView.vue`；用量页拆总览卡为
      「按 Provider」标题行摘要＋四源矮宽三要素卡，按模型表收敛为 7 列；设置页改卡片／行／toggle／
      segmented 形态（640px 窄列、移除「返回用量」由侧边栏承担）；Conversations 源过滤与侧边栏
      全局数据源选中态（内存态、不持久化）联动。vue-tsc、eslint、prettier 与全量 vitest（93 项）
      通过；浏览器与实机验证待门禁。
- [x] 按 [ADR-0028](决策/ADR-0028-全部表面改为贴合式弱化卡片层级.md) 落地贴合式弱化卡片层级
      （2026-08-09）：删 `--shadow-lane`、新增 `--border-hairline`（卡片 1px 低对比描边）与
      `--action-on-primary`；用量页新增 KPI 总览行（总 Token／总花费／各源花费，22px 大数字为
      整页唯一大字号）并删标题行摘要与区块标题装饰横线；侧边栏选中态改蓝底白字；紧凑面板
      lane、设置卡、时间线段去阴影统一 hairline；字号 scale 收敛（11.5px 等奇数小数消除）。
      vue-tsc、eslint、prettier 与相关 vitest 通过；浏览器与实机验证待门禁。
- [x] 按 [ADR-0029](决策/ADR-0029-主窗口与对话页对齐cc-bar的功能与布局.md) 落地主窗口与对话页
      对齐 cc-bar（2026-08-10）：对话标题两级来源（`session_index.jsonl`／`history.jsonl` 优先，
      user 行兜底，新增 `title_index.rs`，cursor 扩展 `source_id`／`pending_title`／Codex
      `project_hint`）；用量页顶栏右上 range 分段＋扫描状态＋手动刷新按钮、KPI 环比 delta
      （`all`／`custom` 除外）、Token 拆分面板与 Fast 汇总；对话页左列表右详情分栏（删
      `conversation-detail` 路由）、时间范围与用量页共享、项目筛选菜单（新增
      `usage_list_conversation_projects`）、行内模型与速度徽标；详情补对话 ID（可复制）与分支；
      schema 升至 v5（`conversations` 增 `source_id`／`branch`）。Rust 241 项与全量 vitest 108 项
      通过，vue-tsc／eslint／prettier 通过；浏览器与实机验证待门禁。存量标题修复需设置页
      「重新计算用量」一次补齐。
- [x] 时间线（Timeline）页面对齐 cc-bar `QuotaTimelineAccountPanel`（2026-08-10）：页面副标题
      「仅展示额度发生变化的时间点」＋今日日期；面板 header 加窗口徽标（5H／WK／MODEL／CURRENT，
      固定短标签不翻译）并将指标改为 Current／Today／Latest（事件数移除）；图表数据点改余量
      四档状态色（`quotaChartColor`，分档仍归 `quotaTone.ts`）、Y 轴只留四档边界刻度
      （0／20／50／80／100）；表格扩为 Time／Change／After／Reset 四列（Change 红涨绿跌、After
      状态色、Reset 为事件时点重置时间）；schema 升至 v6（`quota_events` 增 `resets_at` 列，
      存量行 NULL 显示 `—`）。Rust 全量 242 项与全量 vitest 110 项通过，cargo fmt／clippy、
      vue-tsc／eslint／prettier 通过；浏览器与实机验证待门禁。

## 14. 完成首次初始化

- [x] 创建独立数据目录。（日常使用验证：应用数据落在 `com.nanvon.cctrace` 目录）
- [x] 写入新应用默认设置。（日常使用验证：设置页改动持久化并重启保留）
- [x] 建立新应用缓存。（日常使用验证：`quota-cache.json` 持续读写）
- [x] 写入 `schemaVersion`。（用量库已实际迁移至 v6）
- [ ] 验证原子写入。（损坏恢复路径未验证——已知缺口）
- [ ] 验证缓存损坏时可以回退或重建。（未验证——已知缺口）
- [ ] 验证首次启动不读取旧设置和旧缓存。（审计类，未验证——已知缺口）
- [ ] 验证首次启动不修改旧安全存储项。（审计类，未验证——已知缺口）
- [x] 若包含本地用量，执行原始日志首次全量索引。（2026-08 首次真实索引已在日常使用中实际完成；GB 级压力未专门跑——已知缺口）
- [x] 若包含本地用量，提供进度、错误和中断恢复。（Rust 状态与 command 已实现；真实 GB 级全量扫描未运行）
- [ ] 若原始日志不存在，展示明确空状态。（未验证：日常数据一直存在）

## 15. 业务结果验证

- [ ] 使用脱敏 Provider Fixture 验证 QuotaSnapshot。（未单独执行；解析正确性已由 Rust 单元测试与 2026-07-27 真实数据验证覆盖）
- [x] 验证额度字段和窗口语义。（2026-07-27 实机：窗口类型全部命中、无 `unknown`；日常持续正确）
- [ ] 验证错误分类、节流和 429 退避。（单元测试覆盖；实机未触发——已知缺口）
- [ ] 验证网络失败后的快照保留。（未触发——已知缺口）
- [x] 若包含用量，使用原始 JSONL Fixture 验证聚合结果。
- [x] 若包含 Conversations，验证去重和完整生命周期。（重复 message、半行完成、截断／覆盖重扫、稳定分页契约）
- [x] 若包含 Pricing，验证 Token 和费用口径。（缓存 TTL、Fast／Priority、US 推理、Sol 长上下文、未知价格）
- [x] 必要时与旧版结果做只读对照。（2026-08-08 对照见下方 15.1 节）
- [x] 将差异区分为“新产品决策 / 新实现缺陷 / 旧版遗留行为”。
- [ ] 不使用旧缓存文件作为新应用输入。（审计类，未验证——已知缺口）

### 15.1 真实数据对照记录（2026-08-08，macOS）

用 `scripts/compare_usage.py` 以只读方式对照 CC Trace `usage.db`（`~/Library/Application Support/com.nanvon.cctrace/usage.db`，2026-08-03 上次实扫入库）与 Swift 版 cc-bar `usage-rollup.json`（`~/Library/Application Support/CCBar/`，更新于 2026-08-08）。选定对比日：**2026-08-03**（双方数据都存在且已结束的最后一天）。结果存于临时报告 `/tmp/cc_usage_compare.json`（未入库）。

**一致项（5 行 exact）**：claude 全部（含 `claude-opus-5`、`claude-sonnet-5` 等）与 codex 全部已收录模型，token 六维与 `cost_usd` 逐字段一致；按 Source 总量两套完全相等（claude cost 54.1055755、codex 3.118620724 等）。**核心 Codex／Claude 扫描与计价与旧版一致。**

**差异分类**：

| 差异 | 数量 | 分类 | 说明 |
|---|---|---|---|
| `codex/codex-auto-review/standard` 106 请求 token 全一致、费用均 `$0`；cc-trace 报 `unpriced_entries=106`，ccbar 只有布尔 `has_unpriced_usage=true` | 1 | 旧版遗留／口径差异 | cc-trace 显式统计未定价行数，ccbar 只存布尔标记；数据无实质差异，费用一致为 `$0` |
| pi 三行（`anthropic/claude-opus-5`、`deepseek-v4-flash`、`openai-codex/gpt-5.6-luna`）在 cc-trace 中缺失 | 3 | 新产品决策／时间差 | cc-trace 该库为 2026-08-03 扫描，早于 Pi 数据源实现（当日已进入首版，schema v4）；重新实扫后应由新扫描器纳入，非缺陷 |
| `pricingFingerprint` 不一致（cc-trace `655e45fb…` vs ccbar `096554c7…`） | — | 新产品决策 | 两个目录快照版本不同（cc-trace 库用 08-03 目录）；本对比日 token 与费用全一致，指纹差异未影响该日计价。重新扫描并刷新目录后再复核 |

**未做**：未执行一次全新全量扫描；Pi／OpenCode 数据与 schema v4 的真实对照未单独执行，保持未验证并列入「已知缺口」。

## 16. 双平台验收

- [x] 在 macOS 完成首版功能验收。（日常使用验证，2026-08-10 产品所有者确认）
- [ ] 在 Windows 完成首版功能验收。（已知缺口：无 Windows 主机）
- [x] 对照 `产品范围.md` 确认没有遗漏。（2026-08-10 随文档收尾对照：macOS 侧由日常使用满足；Windows 侧见「已知缺口」）
- [ ] 验证平台差异集中在适配层。（审计类，未验证——已知缺口）
- [ ] 验证安装、启动、重新打开、退出和卸载。（启动／重开／退出已由日常使用覆盖；正式安装与卸载未验证——已知缺口）
- [x] 验证菜单栏或托盘。（macOS 27 日常使用验证）
- [x] 验证开机启动。（设置开启后重启生效，2026-08-10 产品所有者确认）
- [x] 验证系统安全存储。（钥匙串一次授权、日常可用；存储内容审计见「已知缺口」）
- [x] 验证窗口和多显示器行为。（多显示器锚点已有 2026-07-28 实机通过记录；窗口行为日常使用覆盖）
- [ ] 验证中英文、浅色和深色。（中文＋浅色已由日常使用覆盖；英文与深色未专门验证——已知缺口）
- [ ] 验证键盘、焦点和无障碍。（快捷键日常覆盖；焦点环与无障碍未专门验证——已知缺口）
- [ ] 验证新旧应用同时运行时互不影响。（未验证——已知缺口）

## 17. 独立发布

- [x] 确认新应用版本从 `0.x` 演进。
- [x] 首次正式版使用 `1.0.0`。
- [x] 配置 GitHub tag、三处版本一致性门禁与 Draft Release。
- [x] macOS Release 显式选择 Xcode 26 并校验 macOS 26 SDK，不依赖 runner 镜像的默认 Xcode。
- [x] 配置 macOS Apple Silicon、Intel 两个独立 DMG 与 ZIP，内含的 `.app` 使用 ad-hoc 签名且不上传个人证书。
- [x] 配置 Windows x64 未签名 NSIS EXE。
- [x] 配置自动生成 Release Notes 与 `SHA256SUMS.txt`。
- [x] 将新仓库推送到公开 GitHub 仓库，并完成第一次 workflow 实跑。（2026-08-08：`main` 推送
      `d07c4f0`，`v0.1.12` tag 触发 Release workflow，github-actions 于 00:26–00:30 创建公开 Release：
      macOS Apple Silicon／Intel 各 DMG＋ZIP（ad-hoc 签名）与 Windows x64 NSIS EXE（未签名），
      共 5 个分发产物加 `SHA256SUMS.txt`（522 B）。发布页含 GitHub 自动 Release Notes。）
- [ ] 在 Apple Silicon、Intel、Windows x64 真机分别验证 Release 产物。（已知缺口：未做）
- [ ] 验证 macOS Gatekeeper 放行、版本更新后的钥匙串授权，以及 Windows SmartScreen。（已知缺口：未做）
- [x] 在新仓库创建独立 Release。（见上；2026-08-08 已按目标要求把 v0.1.12 Release 转为 **Draft**（`draft: true`），6 个资产保留：5 个分发产物＋`SHA256SUMS.txt`；正式公开时再点 Publish）
- [x] 明确说明新应用不是 Swift 版无损升级。（发布说明已写明 ad-hoc 签名与未签名提示）
- [ ] 明确说明不迁移旧设置、缓存和账号。（正式发布说明补充时处理）
- [ ] 明确说明旧应用不会被自动卸载或清理。（正式发布说明补充时处理）
- [ ] 验证新应用未来版本之间的升级基础。（未验证——已知缺口）
- [ ] 发布后再决定旧 cc-bar 的维护或归档计划。（未做）

## 执行门禁

- [x] 第 1～3 阶段未确认前，不创建正式工程。
- [x] 第 4 阶段未确认前，不设计任何旧数据迁移代码。
- [x] 第 6～8 阶段未确认前，不开始正式业务页面。
- [ ] ~~双平台桌面壳未验证前，不批量实现业务模块。~~ 已由 [ADR-0015](决策/ADR-0015-第12阶段先于桌面壳实机验证启动.md) 撤销。第 11 阶段走查项已于 2026-08-10 由产品所有者确认以 macOS 日常使用验证通过；**Windows 侧任何行为仍不得标记为已验证**。
- [x] 第 5 阶段的 Provider 请求、凭据和错误分类事实未整理前，不凭猜测实现 Provider 解析。（事实已整理为 `docs/额度领域模型.md`）
- [x] 桌面壳预览期的互斥状态枚举未替换为三维契约前，不接入真实 Provider 数据。（三维契约已是最终形态）
- [x] Provider 最小闭环未验证前，不扩展复杂统计能力。（macOS 闭环已由日常使用验证；Windows 缺口不改变该门禁已达成）
- [x] 未进入 `产品范围.md` 的能力，不进入首版实现。
- [x] 每完成一个阶段，同步更新勾选状态和相关文档。
- [x] 旧仓库只读参考；新应用实现和发布均在新仓库完成。

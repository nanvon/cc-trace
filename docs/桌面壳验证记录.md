# Tauri Tray 桌面壳验证记录

> 状态：设置同窗实现与自动化检查已完成，**实机验证待重跑**
> 基线日期：2026-07-26

## 重要：先前的实机结论已失效

2026-07-25 早些时候记录过一次 macOS 实机通过。此后第 11 阶段收口重写了 `lib.rs`、
`platform/tray.rs`、`platform/desktop.rs` 与全部前端视图，并新增了状态契约、调度、
持久化与首次启动窗口。**那次实机结论不再适用于当前实现**，必须按本文件末尾的走查清单
重新跑一遍，通过后再更新本节。

设置同窗改造后的自动化验证已通过（macOS 主机，2026-07-26）：

| 检查 | 结果 |
|---|---|
| `cargo fmt --check` | 通过 |
| `cargo clippy --all-targets -- -D warnings` | 通过 |
| `cargo test` | 46 项全部通过 |
| `pnpm lint`（ESLint + Prettier） | 通过 |
| `pnpm build`（`vue-tsc --noEmit` + `vite build`） | 通过 |
| `pnpm test`（Vitest） | 26 项全部通过 |

这些结果证明当前代码通过 macOS 主机上的格式、lint、类型、构建与单元测试检查，但不等于实机通过：
Tray、窗口锚点、失焦、材质与多显示器仍需要真实桌面会话。

2026-07-25 确认 [ADR-0011](决策/ADR-0011-设置并入主窗口.md)，并于 2026-07-26 完成代码收敛与自动化检查：设置改为主窗口内的二级视图，独立 `settings` 窗口已移除。自动化通过不代表实机验证通过；实机走查前不得恢复“验证已完成”的结论。

## 实现范围

> 2026-07-27 更新：真实 Provider 已按 [ADR-0015](决策/ADR-0015-第12阶段先于桌面壳实机验证启动.md)
> 接入，默认数据来源不再是合成数据。本文件描述的桌面壳行为不受影响，但下方「实现范围」
> 中关于数据来源的两条已改写；实机走查清单仍然全部待跑。

当前实现验证桌面壳与完整状态模型：

- Rust 创建并持有 Tray Icon 与原生菜单，菜单文案随语言设置重建。
- Rust 管理 `main`、`compact`、`onboarding` 三个窗口，设置在 `main` 内导航。
- **三维状态契约已是最终形态**：活动、快照新鲜度、可用性各自独立序列化。
- 刷新编排、请求合并、30 秒节流、429 与网络退避使用真实实现，参数取自
  [技术架构](技术架构.md) 首版参数基线。
- 额度数据默认来自真实 Codex 与 Claude Code Provider；`providers::synthetic` 走完全
  相同的路径，见 [ADR-0009](决策/ADR-0009-合成数据下沉Rust并提前使用正式契约.md)。
- `settings.json` 与 `quota-cache.json` 已持久化：前者存语言、外观、刷新间隔、开机启动、
  首次启动完成标记，见 [ADR-0010](决策/ADR-0010-最小设置持久化提前落地.md)；后者存每个
  Provider 的脱敏快照与身份指纹。
- 读取外部凭据，并在 token 续期成功时回写同一来源，见
  [ADR-0014](决策/ADR-0014-token刷新结果回写外部凭据.md)。不读取外部日志或旧 cc-bar 数据。
- 不创建日志系统。

debug 构建提供 `dev_set_scenario` 与主窗口底部的场景切换器，覆盖真实数据加 9 个合成
验证场景；默认停在「真实数据」，release 构建里两者都不存在。

## Rust 与 Vue 边界

| 能力 | 所属层 |
|---|---|
| Tray Icon、原生菜单、主点击事件、菜单本地化 | Rust |
| 窗口显示、聚焦、失焦隐藏、关闭不退出、锚点定位、主点击去抖 | Rust |
| macOS 模板图标与 Windows Bundle Icon | Rust 平台分支 |
| 三维状态、刷新编排、节流、退避、场景 | Rust |
| 设置持久化与开机启动 | Rust |
| 三个窗口的界面、状态视觉表达、i18n、键盘；主窗口包含额度与设置两个视图 | Vue |
| 打开窗口、刷新、退出请求 | Vue 调用窄 Tauri command |

前端没有取得任意窗口创建、通用文件系统、Shell、HTTP 或秘密读取能力。
Capability 见 [技术架构](技术架构.md)「凭据与权限」。

## 系统入口

### macOS

- 使用 `ActivationPolicy::Accessory`，不占用 Dock。
- 使用 CC Trace Tray Symbol 的 RGBA 模板图标，随 Menu Bar 外观着色。
- 左键松开时打开／关闭紧凑窗口，带 250ms 去抖。
- 紧凑窗口按 Tray **图标矩形**（不是光标位置）定位到图标正下方，并在锚点所在显示器的
  工作区内约束。坐标全程用逻辑点，见下方 2026-07-28 的实机发现。
- 失焦时自动隐藏紧凑窗口。
- 右键显示原生菜单。
- 紧凑面板使用透明窗口 + `popover` 材质 + 16px 圆角（需要 `macos-private-api`）。

### Windows

- 使用应用 Bundle Icon 作为 System Tray 图标。
- 使用与 macOS 相同的左键紧凑入口语义与去抖。
- 使用右键原生菜单。
- 紧凑窗口定位到 Tray **图标矩形**上方，并约束在锚点所在显示器工作区。坐标沿用物理
  像素：Windows 的物理坐标是真实像素，全程可比，不需要 macOS 那套逻辑点换算。
- 紧凑面板是透明窗口但不套材质，内容层实色；`windowEffects` 的 `popover` 在
  Windows 被忽略。**WebView2 下透明窗口的对比度与性能未验证**，若表现不佳需要
  回退为不透明外壳并记录。

Windows 分支只做过条件编译，没有 Windows 主机，因此不得标记为实机通过。

## 主点击去抖

点击系统区域图标时，紧凑面板会先失焦（从而隐藏），随后 tray 的点击事件才到达。
只看 `is_visible()` 会让第二次点击变成「隐藏后立刻重新显示」，表现为面板关不掉。

当前实现记录最近一次隐藏时刻，250ms 内的点击一律视为「关闭」。这是必须重点回归的
交互：**连续点击图标 5 次，面板应当开、关、开、关、开**。

## 窗口策略

| 窗口 | 初始状态 | 关键行为 |
|---|---|---|
| `compact` | 隐藏 | 无装饰、透明、不可缩放、置顶、不进任务栏、失焦隐藏、`Esc` 关闭 |
| `main` | 隐藏 | 额度与设置同窗切换；打开已存在实例并聚焦；关闭时隐藏，进程继续驻留 |
| `onboarding` | 隐藏 | 仅在完成标记缺失时启动打开；完成后隐藏并打开紧凑面板 |

三个窗口都由配置预创建，重复操作不会创建第二个实例。进程级单实例由
`tauri-plugin-single-instance` 保证：首次启动未完成时二次启动聚焦引导；完成后聚焦主窗口额度总览。

## 键盘

快捷键在前端统一处理 `metaKey || ctrlKey`，因此业务代码不做平台判断：

| 快捷键 | 行为 |
|---|---|
| `Esc` | 关闭紧凑面板（仅紧凑面板） |
| `⌘R` / `Ctrl+R` | 刷新全部额度 |
| `⌘,` / `Ctrl+,` | 聚焦主窗口并打开设置视图 |
| `⌘W` / `Ctrl+W` | 关闭当前窗口，应用继续驻留 |
| `⌘Q` / `Ctrl+Q` | 退出应用 |

因为是 Accessory 应用，没有应用菜单栏，快捷键只在某个窗口聚焦时生效。这符合菜单栏
应用的常见范式，但需要在实机确认没有与系统快捷键冲突。

## 待重跑的实机走查清单

macOS 与 Windows 各跑一遍，结果连同平台与日期写回本文件：

- [ ] 设置入口从额度总览、紧凑面板、原生菜单和快捷键进入同一个 `main` 窗口；返回额度、焦点恢复、窗口尺寸与位置稳定。
- [ ] 首次启动完成后，“打开 CC Trace”和二次启动始终落到额度总览，不恢复上次设置视图；全程不创建独立 `settings` 窗口。
- [ ] 菜单栏／托盘图标在浅色、深色与高对比下可辨识。
- [x] macOS 菜单栏徽标（ADR-0017）：缩小与模糊的渲染链根因已定位。Tauri 底层把任意
      图片统一显示为 18pt；原 22pt／44px 画布因此被再次缩小。2026-07-28 已改为最终
      18pt／36px 画布并原子设置 template。
- [x] macOS 菜单栏徽标：字体族解析。实测 `SF Pro` / `.SF NS Text` / `.AppleSystemUIFont`
      都命中系统 UI 字体；AppKit 的真实菜单栏字体为 13pt Regular。`SF Pro Text` 不存在
      会静默回退 Helvetica。字体解析与 36px Retina 输出均有单元测试守住。
- [ ] macOS 菜单栏徽标：第三轮光学校准后的实际观感——当前以 19pt 标识、13.5pt Medium
      文字补偿 `resvg`／`tiny-skia` 相对 AppKit／Core Text 偏小、偏细的实际墨量；继续与
      cc-bar 对照尺寸、清晰度和字重。
- [ ] macOS 菜单栏徽标：模板着色在浅色与深色菜单栏下都正确，标识与数字都可辨识。
- [ ] macOS 菜单栏徽标：百分比从三位数变到一位数时，菜单栏 item 像 cc-bar 一样
      按当前文字自然改变宽度；图标与文字保持 3pt，两段之间保持 9pt，没有额外固定空白。
- [ ] macOS 菜单栏徽标：无凭据、离线、首次加载时显示 `--` 且图标不消失。
- [ ] 卡片主行与系统区域读取同一个第一项：Codex 显示 weekly，Claude Code 显示
      fiveHour；两者都不按窗口类型或旧 `isPrimary` 标记重选。
- [ ] tooltip 显示「Codex 62% · Claude Code 78%」形态，随刷新更新。
- [ ] Windows：托盘 tooltip 是额度的唯一载体，确认它随刷新更新且长度不被截断。
- [ ] 左键连续点击 5 次，面板正确开关（去抖回归）。
- [ ] 点击面板外部关闭；`Esc` 关闭并把焦点交回。
- [ ] 面板打开时焦点落在「用量」标题且不出现按钮蓝框；按 `Tab` 后进入刷新按钮并显示
      正常的键盘焦点环。
- [ ] 右键原生菜单四项顺序与职责正确，语言随设置切换。
- [ ] 「查看详情」关闭面板并打开主窗口；重复打开只聚焦不新建。
- [ ] 关闭所有窗口后进程与 Tray 图标继续存在。
- [ ] 逐一走查 9 个验证场景，确认：`no_credentials` 不显示 `0%`、`stale` 保留数值、
      429 显示可再试时间、单个 Provider 失败不影响另一个。
- [ ] 余量四档色（ADR-0017）：读数与进度条在 `> 50`、`20–50`、`< 20`、`= 0` 四档下颜色
      正确，`stale` 时降级为中性；浅色与深色都要看。
- [ ] 紧凑面板头部四个图标按钮：Tooltip 与 VoiceOver／讲述人名称正确，Tab 顺序合理，
      32 × 32 点击区域实际可点。
- [ ] 紧凑面板自动高度：稳态（Claude 三个额度窗口、Codex 两个）下面板底部没有多余留白，
      实测高度与 `docs/设计方向与状态规范.md` 第 5.3 节的 `392` 计算值对齐；偏差写回该节。
- [ ] 紧凑面板自动高度：两个 Provider 都带提示条时触到 `560` 上限，概览区滚动而头部不动。
- [ ] 紧凑面板自动高度：面板打开状态下额度刷新导致高度变化时，不出现闪烁或反复抖动
      （量测与窗口尺寸互相触发会表现为持续抖动）。
- [ ] Windows：面板高度变化时底边始终贴住托盘上方，不向下长出去盖住托盘图标。
- [ ] Windows：实测稳态高度（系统字体行高更大，预期约 `400`），与 macOS 的差值写回第 5.3 节。
- [ ] 手动刷新在退避期内不发起请求，只显示可再试时间。
- [ ] 设置四项均可改并立即生效；重启后保留。
- [ ] 删除 `settings.json` 后重新出现首次启动窗口；手动写坏该文件后回退默认值
      且生成 `settings.json.corrupt`。
- [ ] 开机自动启动开关在系统层真实生效。
- [ ] 二次启动只聚焦已有实例，不出现第二个托盘图标。
- [ ] 深色／浅色、200% 文字缩放、reduced motion。
- [x] 多显示器与不同缩放比例下的面板锚点。2026-07-28 在 macOS（内建 Retina 2x + 1x
      外接，混合缩放）通过：面板落在图标正下方，在哪块屏点击就在哪块屏打开。修复过程
      见下方实机发现。**Windows 未验证。**
- [ ] Windows：透明窗口在 WebView2 下的对比度与性能。
- [ ] Windows：关闭主窗口后是否需要一次驻留提示。

## 尚未完成

- 上述实机走查清单（macOS 需重跑，Windows 从未跑过）。
- GitHub Release 打包已配置但尚未实跑：Apple Silicon／Intel DMG 使用 ad-hoc 签名，Windows x64 使用未签名 NSIS；三个产物均待对应平台实机验证。
- ad-hoc 发布产物升级后，首次读取 Claude Code 钥匙串是否按预期重新授权一次，尚未验证。
- 应用数据目录与系统安全存储的审计。
- 新旧应用并行运行与目录访问审计。
- Vue 层不能取得秘密——Provider 已接入，现在可以验证，尚未做。

以上项目完成前，第 11 阶段保持「进行中」。第 12 阶段已按
[ADR-0015](决策/ADR-0015-第12阶段先于桌面壳实机验证启动.md) 并行启动，但这些走查项
**不因此视为已通过**。

## 实机发现（macOS，2026-07-27）

来自真实运行的结论，不是自动化检查结果。

### 已通过

**两个 Provider 的真实额度闭环跑通**，紧凑面板与主窗口都正确展示：

| 项 | 结果 |
|---|---|
| Codex 凭据发现 | `~/.codex/auth.json`，OAuth 凭据可用 |
| Claude Code 凭据发现 | **走的是 macOS 钥匙串**（本机没有 `.credentials.json`），[ADR-0013](决策/ADR-0013-macOS读取ClaudeCode钥匙串凭据.md) 成立 |
| 身份脱敏 | 两个 Provider 都显示为 `n***@gmail.com`，command 载荷不含完整邮箱 |
| 计划名 | Codex `plus`、Claude Code `pro`，均从本地凭据解析 |
| 窗口判定 | 全部命中已知类型，**没有出现 `unknown`** |
| Provider 返回顺序 | Codex 第一项是 weekly；Claude Code 第一项是 fiveHour，后续是 weekly |

### 待复核的真实响应形态

两项与静态审计预期不同，原因未知，**不猜**：

- **Codex（plus）只返回一个 weekly 窗口，没有 5 小时窗口。** 与 [cc-bar 只读参考](cc-bar-reference/04-账号凭据Provider与额度协议.md)「`limit_window_seconds` 推断 5H/weekly」的预期形态不同。可能是 API 变化、plus 计划本就如此，或 5 小时窗口无用量时不返回。
- **Claude Code（pro）没有 `modelWeekly` 窗口**，只有 fiveHour 与 weekly。模型级额度可能是 max 计划才有。

因此 `weekly_scoped` 的 scope 派生 ID 这条路径**在真实数据上仍未验证**，只有 Fixture 覆盖。

2026-07-28 已把卡片主行与系统区域统一改为读取上述第一项；改动后的菜单栏实际显示仍需重启开发构建复核。

### 钥匙串授权

**每次重新编译后都会重新要求授权**（2026-07-27 发现）。dev 构建是 adhoc 签名，没有稳定签名标识，钥匙串 ACL 只能按 CDHash 记录授权对象，而 CDHash 随二进制内容变化。详见 [ADR-0013](决策/ADR-0013-macOS读取ClaudeCode钥匙串凭据.md) 的实机记录。

**2026-07-28 已解决。** `scripts/dev-sign.sh` 作为 cargo runner 在启动前用固定身份重签，签名标识钉死成 `tauri.conf.json` 的 `identifier`。实测授权一次后重新编译不再弹窗。用的是免费 Apple ID 的 Apple Development 证书而非原计划的自签名证书——后者的 designated requirement 绑定证书指纹，换证书即失效，见 [ADR-0016](决策/ADR-0016-不购买Apple开发者账号.md) 的 2026-07-28 修订。

开发期与 GitHub 发布现在使用不同策略：开发期保留 Apple Development 证书，公开仓库的
Release workflow 使用 ad-hoc 签名且不上传个人证书。后者的 CDHash 会随版本变化，因此安装
新版本后首次访问钥匙串可能需要再次选择「始终允许」；这是已接受的发布代价，实际安装包仍
需在对应架构真机验证。

2026-07-28 已补 macOS 专属的首次授权预告，并把首次 Provider 刷新改为由用户点击
「检查本机」后触发，避免系统弹窗先于解释出现。文案与时序已落代码，仍需用全新首次启动
状态实机复核。

### 本轮修复的缺陷

- 「合成数据」标题与「当前数值不来自 Codex 或 Claude Code」这句免责文案原先**无条件渲染**，既没有 dev 判断也不跟随数据来源，会挂在真实额度下面，打包后真实用户同样可见。已改为只在 debug 构建且确实切到合成场景时出现，紧凑面板那句直接移除。
- `.utility-label` 的 `letter-spacing: 0.08em` 是配 `text-transform: uppercase` 给大写拉丁字母用的，套在「5 小时窗口」这类中文标签上明显松散。已按 `:lang(zh)` 归零。

## 实机发现（macOS，2026-07-28）：紧凑面板锚点

来自真实运行的结论，不是自动化检查结果。

### 症状

面板不在菜单栏图标正下方；在外接显示器点击图标时，面板落到主屏而不是外接屏。

### 根因：macOS 上 Tauri 的「物理坐标」不是统一坐标系

macOS 上 Tauri／tao 的 physical 值实际是「逻辑点 × **被问对象自己的** scale factor」。
定位链上四个环节各按各的算，单显示器时 scale 相同、恰好互相抵消，多显示器（尤其
混合 Retina 与 1x）立刻散架：

| 环节 | 实际含义 |
|---|---|
| tray 事件的 `position` / `rect` | 逻辑点 × **托盘所在屏**的 scale |
| `monitor_from_point(x, y)` | 参数直接与**纯逻辑**的显示器边界比较，不做任何换算 |
| `monitor.work_area()` | 逻辑点 × **该显示器**的 scale |
| `set_position(Physical)` | 除以**窗口当前所在屏**的 scale 后当逻辑点用 |

最后一条是「跑回主屏」的直接原因：面板上次停在 2x 主屏，为 1x 外接屏算出的坐标被
除以 2，正好落回主屏中部。

同时锚点取的是事件里的 `position`（光标位置）而非 `rect`（图标矩形），面板水平位置
跟着「点中图标的哪个像素」漂移，顶边还会压进菜单栏。

### 处理

`platform/desktop.rs` 引入 `Anchor` 与 `to_anchor_space()`：macOS 全程换算成逻辑点，
最后用 `Position::Logical` 输出；Windows 恒等，保持物理像素。锚点改用图标矩形。

`tray-icon` 只给出按托盘所在屏换算后的矩形，**不告诉我们是哪块屏**，且数值区间无法
区分——逻辑 x=2500 在 1x 屏上是 2500，按 2x 反推出的 1250 同样落在主屏范围内。因此
`tray_rect_scale()` 从图标高度反推：菜单栏厚度是全系统统一的逻辑值（现代 macOS 为
24pt），Mac 的 backing scale factor 只有 1 或 2。这个前提如果哪天不成立，退化结果是
回到修复前的行为。

### 已验证

macOS 内建 Retina 2x + 1x 外接（混合缩放，scale 反推的两个分支都走到）：

- 面板落在菜单栏图标正下方。
- 在哪块屏点击就在哪块屏打开，不再跑回主屏。
- 额度刷新导致面板高度变化后，位置仍锚在图标下。

**Windows 全部未验证。** 那边的物理坐标是真实像素，换算路径是恒等的，改动只多了
「锚定图标中心而非光标」这一项，但没有 Windows 主机，不得标记为通过。

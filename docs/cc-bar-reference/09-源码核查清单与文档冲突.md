# 09 · 源码核查清单与文档冲突

## 审计快照

| 项 | 结果 |
|---|---:|
| 审计日期 | 2026-07-24 |
| 当前分支 | `main` |
| 当前提交 | `d47e39f` |
| 基准 scope 文件数 | 97 |
| 已检查文件数 | 97 |
| 未读 / 无法读取 | 0 |
| 额外引用文档 | 1，不计入基准 scope |
| XCTest 测试方法 | 25 |
| JSONL Fixture | 2 |
| 已记录主要文档冲突 | 7 |
| 已列待产品确认问题 | 12 |
| 本次构建/测试/运行 | 0 次 |

scope 由仓库文件清单建立，排除了 `build/`、`dist/`、`.git/`、`docs/历史参考/` 和 `.DS_Store`，再限定为入口、构建配置、Core、当前 UI、测试、资源、指定文档和截图。**代码已确认**：范围内二进制 PNG 以 `file`/图像检查其格式、尺寸和用途；文本文件逐项读取。

## 审计快照后的新增（2026-07-20 `d47e39f` 之后，截至 2026-08-07 `dfbf76d`）

下表未计入上方基准 scope 与计数，是快照后各次提交引入的事实，已分别同步到对应参考文档与 cc-trace 侧数据源文档：

| 提交 | 内容 | 事实同步位置 |
|---|---|---|
| `7420786`（2026-08-03） | `Core/Usage/PiJSONLScanner.swift`、`CCBarTests/PiJSONLScannerTests.swift`（+192 行）、`Resources/Assets.xcassets/PiAccent.colorset`、`UsageApp.pi`、`ScanState` v10 | 参考文档 [01](01-产品定位与功能清单.md) 边界 6；cc-trace [Pi数据源](../Pi数据源.md) |
| `a22fa34`（2026-08-03） | 每日用量柱图由费用改为按 `totalTokens` 展示 | 参考文档 [01](01-产品定位与功能清单.md) F-16、[02](02-页面窗口与交互地图.md) Overview、[05](05-本地用量对话与计价口径.md) UI 消费口径 |
| `49ad45c`（2026-08-07） | `Core/Usage/OpencodeScanner.swift`（+332 行）、`CCBarTests/OpencodeScannerTests.swift`（+351 行，10 个 SQLite fixture 测试）、`Resources/Assets.xcassets/OpencodeAccent.colorset`、`UsageApp.opencode`、`ScanState` v11、Pricing/CatalogStore 短路、AppState.opencodeTodayCost | 参考文档 [01](01-产品定位与功能清单.md) F-27/边界 7、[05](05-本地用量对话与计价口径.md) 价格短路；cc-trace [OpenCode数据源](../OpenCode数据源.md) |
| `1023969`／`5949687`（2026-08-07） | 统计页按服务过滤（`Settings.usageServiceVisibility`、`StatsView`、`SettingsRootView`），By service 面板 >3 个服务两列换行、KPI 单行 | 参考文档 [01](01-产品定位与功能清单.md) F-28、[02](02-页面窗口与交互地图.md) Overview/Settings、[05](05-本地用量对话与计价口径.md) UI 消费口径 |
| `f595115`（2026-08-08） | 文档对齐代码（**无代码变化**）：Popover 改为 32pt 大数字 + 7pt 进度条（56pt 进度环移除）、HUD 三层材质、进度环标注废弃、设置页／Onboarding／主窗口按实际 UI 重写并删除未实现设置项（8 变体／位置／透明度／showInDock／autoRefresh）、scan-state 改 v11、normal 档色值修正 | 本文件下方冲突清单 C-03/C-04/C-06/C-07 标记已解决；[02](02-页面窗口与交互地图.md) Popover/设置、[06](06-状态持久化调度与错误恢复.md) 磁盘表与幽灵设置项标注同步 |

快照表的「XCTest 测试方法 25」「JSONL Fixture 2」「Core 39 个文件」等计数是审计时点值，不随上述提交滚动更新；引用时需叠加本表。

## 范围分组计数

| 分组 | 数量 | 已核查 |
|---|---:|---:|
| 入口 / 构建 / 工程 / 发布 / Helper | 9 | 9 |
| `Core/` | 39 | 39 |
| `MenuBar/`、`Main/`、`Settings/`、`Floating/`、`Onboarding/` | 14 | 14 |
| `CCBarTests/`（含 Fixture） | 3 | 3 |
| `Resources/`（Logos、MenuBarIcon、Assets.xcassets） | 19 | 19 |
| 当前 docs 与 docs/Screenshots | 12 | 12 |
| 根目录 README | 1 | 1 |
| **合计** | **97** | **97** |

## 逐文件核查清单

### 入口、构建和发布（9）

| 状态 | 文件 |
|---|---|
| ✅ | `.github/workflows/release.yml` |
| ✅ | `AppDelegate.swift` |
| ✅ | `CCBar.entitlements` |
| ✅ | `CCBarApp.swift` |
| ✅ | `Info.plist` |
| ✅ | `ccbar.xcodeproj/project.pbxproj` |
| ✅ | `ccbar.xcodeproj/xcshareddata/xcschemes/ccbar.xcscheme` |
| ✅ | `scripts/build.sh` |
| ✅ | `Helpers/CCBarClaudeWatchdog/main.swift` |

核查结论：单 macOS App target + unit test target；deployment target 14.0；`SWIFT_VERSION=5.0`；app sandbox 关闭；release tag workflow 调仓库脚本打包；Claude watchdog 作为 helper 编译嵌入。**代码已确认**。

### Core（39）

| 状态 | 文件 |
|---|---|
| ✅ | `Core/AppState.swift` |
| ✅ | `Core/Credentials/ClaudeAuth.swift` |
| ✅ | `Core/Credentials/CodexAuth.swift` |
| ✅ | `Core/Credentials/ImportedCodexAccount.swift` |
| ✅ | `Core/Credentials/ImportedCodexStore.swift` |
| ✅ | `Core/Credentials/JWT.swift` |
| ✅ | `Core/Credentials/Models.swift` |
| ✅ | `Core/L10n.swift` |
| ✅ | `Core/Pricing/LiteLLMPricingDecoder.swift` |
| ✅ | `Core/Pricing/ModelsDevPricingDecoder.swift` |
| ✅ | `Core/Pricing/PricingCatalogModels.swift` |
| ✅ | `Core/Pricing/PricingCatalogStore.swift` |
| ✅ | `Core/Quota/AntigravityQuotaClient.swift` |
| ✅ | `Core/Quota/ClaudeCLIFallbackQuotaClient.swift` |
| ✅ | `Core/Quota/ClaudeDelegatedRefresh.swift` |
| ✅ | `Core/Quota/ClaudeQuotaClient.swift` |
| ✅ | `Core/Quota/ClaudeTokenRefresher.swift` |
| ✅ | `Core/Quota/CodexQuotaClient.swift` |
| ✅ | `Core/Quota/CodexResetCreditsClient.swift` |
| ✅ | `Core/Quota/CodexTokenRefresher.swift` |
| ✅ | `Core/Quota/QuotaModels.swift` |
| ✅ | `Core/Quota/ServiceStatusClient.swift` |
| ✅ | `Core/Scheduler.swift` |
| ✅ | `Core/Storage/ConversationRollupCache.swift` |
| ✅ | `Core/Storage/ImportedUsageBackfill.swift` |
| ✅ | `Core/Storage/QuotaCache.swift` |
| ✅ | `Core/Storage/QuotaHistoryStore.swift` |
| ✅ | `Core/Storage/ScanCache.swift` |
| ✅ | `Core/Storage/Settings.swift` |
| ✅ | `Core/Usage/ClaudeJSONLScanner.swift` |
| ✅ | `Core/Usage/CodexJSONLScanner.swift` |
| ✅ | `Core/Usage/ConversationAggregator.swift` |
| ✅ | `Core/Usage/ConversationModels.swift` |
| ✅ | `Core/Usage/ConversationTitleIndex.swift` |
| ✅ | `Core/Usage/JSONLLineReader.swift` |
| ✅ | `Core/Usage/Pricing.swift` |
| ✅ | `Core/Usage/UsageAggregator.swift` |
| ✅ | `Core/Usage/UsageModels.swift` |
| ✅ | `Core/Usage/UsageService.swift` |

核查结论：Core 覆盖凭据、统一额度、三类 Provider、调度、设置、缓存、JSONL 增量扫描、对话聚合、Pricing 和全局协调；具体事实分布在 [03](03-架构模块与核心流程.md)、[04](04-账号凭据Provider与额度协议.md)、[05](05-本地用量对话与计价口径.md)、[06](06-状态持久化调度与错误恢复.md)。**代码已确认**。

### 当前 UI（14）

| 状态 | 文件 |
|---|---|
| ✅ | `MenuBar/MenuBarLabel.swift` |
| ✅ | `MenuBar/OtherCodexAccountsSection.swift` |
| ✅ | `MenuBar/PopoverRootView.swift` |
| ✅ | `Main/ConversationStatsView.swift` |
| ✅ | `Main/DesignSystem.swift` |
| ✅ | `Main/MainWindowRootView.swift` |
| ✅ | `Main/StatsView.swift` |
| ✅ | `Settings/CodexResetCreditsDetailView.swift` |
| ✅ | `Settings/ImportedCodexAccountsView.swift` |
| ✅ | `Settings/SettingsRootView.swift` |
| ✅ | `Floating/FloatingContentView.swift` |
| ✅ | `Floating/FloatingPanel.swift` |
| ✅ | `Floating/FloatingPanelController.swift` |
| ✅ | `Onboarding/OnboardingView.swift` |

核查结论：实际 UI 已有 Popover、主窗口三类统计视图、设置、导入 sheet、Reset Credits、固定两行 HUD 和四步引导；设计文档中更丰富的 HUD/菜单栏 variant 选项没有落成对应代码。**代码已确认 / 文档声称**。

### 测试和 Fixture（3）

| 状态 | 文件 |
|---|---|
| ✅ | `CCBarTests/QuotaParsingTests.swift` |
| ✅ | `CCBarTests/Fixtures/claude-fast-scan.jsonl` |
| ✅ | `CCBarTests/Fixtures/codex-fast-scan.jsonl` |

核查结论：1 个 XCTest 文件、25 个测试方法、2 个脱敏 JSONL；没有 AppState、网络 mock、Keychain、AppKit 生命周期、Antigravity、Scheduler 或 Windows 测试。**代码已确认**，详见 [07](07-测试现状与TDD行为基线.md)。

### 资源（19）

| 状态 | 文件 |
|---|---|
| ✅ | `Resources/Assets.xcassets/Contents.json` |
| ✅ | `Resources/Assets.xcassets/AccentColor.colorset/Contents.json` |
| ✅ | `Resources/Assets.xcassets/AntigravityAccent.colorset/Contents.json` |
| ✅ | `Resources/Assets.xcassets/ClaudeAccent.colorset/Contents.json` |
| ✅ | `Resources/Assets.xcassets/CodexAccent.colorset/Contents.json` |
| ✅ | `Resources/Assets.xcassets/AppIcon.appiconset/Contents.json` |
| ✅ | `Resources/Assets.xcassets/AppIcon.appiconset/icon_16.png` |
| ✅ | `Resources/Assets.xcassets/AppIcon.appiconset/icon_32.png` |
| ✅ | `Resources/Assets.xcassets/AppIcon.appiconset/icon_64.png` |
| ✅ | `Resources/Assets.xcassets/AppIcon.appiconset/icon_128.png` |
| ✅ | `Resources/Assets.xcassets/AppIcon.appiconset/icon_256.png` |
| ✅ | `Resources/Assets.xcassets/AppIcon.appiconset/icon_512.png` |
| ✅ | `Resources/Assets.xcassets/AppIcon.appiconset/icon_1024.png` |
| ✅ | `Resources/Logos/codex.svg` |
| ✅ | `Resources/Logos/claude.svg` |
| ✅ | `Resources/Logos/antigravity.svg` |
| ✅ | `Resources/MenuBarIcon/menubar-icon.svg` |
| ✅ | `Resources/MenuBarIcon/menubar-22.png` |
| ✅ | `Resources/MenuBarIcon/menubar-44.png` |

核查结论：Xcode Asset Catalog 有三套 Provider accent、AppIcon 多分辨率 PNG；运行时 UI 通过 `Bundle.main` 读取三份 SVG logo，菜单栏图像为 template；截图资源仅用于文档展示。**代码已确认**。

### 当前文档和截图（12）

| 状态 | 文件 |
|---|---|
| ✅ | `docs/README.md` |
| ✅ | `docs/产品需求.md` |
| ✅ | `docs/技术实现.md` |
| ✅ | `docs/设计风格.md` |
| ✅ | `docs/界面布局.md` |
| ✅ | `docs/草案-跨端重构方案.md` |
| ✅ | `docs/Tauri桌面端重新开发执行清单.md` |
| ✅ | `docs/Screenshots/popover-light.png` |
| ✅ | `docs/Screenshots/popover-dark.png` |
| ✅ | `docs/Screenshots/statistics-overview.png` |
| ✅ | `docs/Screenshots/statistics-conversations.png` |
| ✅ | `docs/Screenshots/statistics-timeline.png` |

核查结论：产品/技术/设计/布局文档描述当前 Swift 版，但跨端两份草案明确新版独立、全新 UI、独立数据且 macOS/Windows 双平台。截图展示的 Popover、Overview、Conversations、Timeline 与当前 UI 方向一致，可作视觉参考而不能代替代码事实。**文档声称 / 代码已确认**。

### 根 README（1）

| 状态 | 文件 |
|---|---|
| ✅ | `README.md` |

核查结论：README 说明 macOS 14+、Codex/Claude/Antigravity、HUD、导入账号和本地 Token/cost，并指向截图和脚本发布方式。**文档声称**。

## 额外读取但不计入 scope 的引用文档

| 文件 | 为什么读取 | 结论 |
|---|---|---|
| `docs/历史用量数据补录指南.md` | `Core/Storage/ImportedUsageBackfill.swift` 的源码注释显式指向它 | 这是一次性外部备份补录手册，不是当前产品入口；源码只在应用支持目录发现固定 backfill JSON 后导入缺失 Claude 日，不改业务代码。**代码已确认 / 文档声称** |

历史参考目录和品牌目录没有计入基准范围；它们可作背景，但不应改变当前代码审计计数。

## 主要文档冲突（7）

| ID | 冲突 | 当前结论 | 严重度 |
|---|---|---|---|
| C-01 | `docs/技术实现.md` 写“Swift 6”；Xcode build settings 是 `SWIFT_VERSION = 5.0`。 | 以工程配置为准；新版文档不要把“Swift 6”当作当前事实。 | 高 |
| C-02 | `docs/界面布局.md` 写主窗口默认 1080×680、最小 800×520；`CCBarApp.swift` 默认 1280×680，`MainWindowRootView.swift` 最小 1040×520。 | 已解决（2026-08-08）：`界面布局.md` 同步为默认 1600×1100、最小 1040×520，与代码一致。 | 中 |
| C-03 | 布局/设计文档写 HUD 有 8 个 variant、位置、空闲透明度，并列出 `menuBarVariant`、`floatingPosition`、`floatingIdleOpacity`、`autoRefreshEnabled`、`showInDock` 等字段；`SettingsStore` 和 `SettingsRootView` 没有这些实际字段/控件。 | 已解决（2026-08-08 `f595115`）：cc-bar 文档删除全部未实现设置项描述，与代码一致；当前只实现固定 Two-row pill、Provider 行开关、floatingEnabled、拖动保存 frame。 | 高 |
| C-04 | 布局文档说 HUD 不加 strokeBorder，并标注现有 stroke 应去掉；`FloatingContentView.swift` 当前确实绘制了 0.5pt `strokeBorder`。 | 已解决（2026-08-08 `f595115`）：cc-bar 文档改为「保留 0.5pt `Color.primary.opacity(0.12)` hairline 描边」，与代码一致。 | 中 |
| C-05 | 设计/布局文档多处以 macOS 26 Tahoe HIG 为基准；工程 deployment target 是 macOS 14.0。 | 设计文档的系统版本假设不应当作当前最低运行版本。 | 中 |
| C-06 | 产品/技术文档把 Codex 与 Claude token skew 都概括为 5 分钟；当前 Codex 是 300 秒，Claude `ClaudeTokenRefresher` 是 30 秒。 | 已解决（2026-08-08 `f595115`）：cc-bar 产品文档改为 Codex 5 分钟内、Claude 30 秒内（被动优先），与代码一致；新版应把 skew 写成 Provider policy。 | 高 |
| C-07 | 布局正文历史段落曾要求刷新中 disabled，且 header 顺序描述有旧版本差异；当前 Popover 刷新按钮不 disabled，实际顺序为 refresh → stats → settings → power。 | 已解决（2026-08-08 `f595115`）：cc-bar 布局文档改为刷新按钮不禁用（`refreshNow` 内部 in-flight 去重）、顺序 refresh → stats → settings → power，与代码一致。 | 中 |

### 已解决的历史性说明

`docs/产品需求.md` 的“一期明确不做”后面已经补充说明：Claude CLI fallback 实际作为最后兜底实现。它不再是未决冲突，但仍必须在新版 Provider 范围中重新决定。**文档声称 + 代码已确认**。

## 未决问题（12）

这些问题不是本次审计遗漏，而是跨端产品需要用户/产品负责人确认的决策；集中列在 [08-跨端复用决策矩阵](08-跨端复用决策矩阵.md)：

1. Antigravity 是否进入新版 v1。
2. 本地用量是否进入 v1。
3. v1 的 quota window 范围。
4. 自动刷新和 429 参数是否沿用旧版。
5. 是否保留 CLI fallback/delegated refresh。
6. 是否进入 imported Codex accounts。
7. 是否显示 public service status。
8. 是否做跨平台 HUD。
9. Pricing/API 等值费用是否进入 v1。
10. 外部日志的路径、标题、对话 ID 隐私范围。
11. 新旧应用并行运行时的用户提示和安装体验。
12. Windows Provider 凭据、日志、进程路径和脱敏 Fixture 的事实来源。

## 改动边界核对

本次允许的写入只有 `docs/cc-bar-reference/` 下 10 个 Markdown 文件：本 README 加 9 个主题文档。没有修改 Swift、Xcode 工程、plist、entitlements、脚本、测试、Fixture、资源或既有文档；没有 git add/commit/push；没有运行 build、test、lint、type-check、dev、App、模拟器或真实 Provider 请求。**审计边界**。

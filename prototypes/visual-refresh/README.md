# 视觉方向 + 主窗口导航形态评审稿

> **状态（2026-08-03）**：产品所有者确认本稿为后续重构应用风格样式时的**唯一参考资料**，
> 实现时按本稿**一模一样还原**。视觉部分 🚩 取值与现行文档（[ADR-0017](../../docs/决策/ADR-0017-系统区域显示额度数字与余量分档.md) ok 档色、[设计方向与状态规范](../../docs/设计方向与状态规范.md) canvas/阴影/字号）的冲突清单与定稿门槛见 [`docs/视觉修复方向评审.md`](../../docs/视觉修复方向评审.md)，还原实现前需完成该文档的定稿步骤（补 ADR、同步 DESIGN.md／设计方向、改 `src/styles/tokens.css`）。

本目录是 [ADR-0012](../../docs/决策/ADR-0012-视觉方向改为HeroUI风格.md) 视觉方向的**修复版评审稿**，并已合并主窗口**未来导航形态**（分组侧边栏）的评审，不是新的视觉方向，也不属于 `src/` 正式应用。

直接打开 [`index.html`](./index.html) 即可并排检查浅色／深色下的：

- 紧凑面板（popover）。
- 主窗口本地用量页（带分组侧边栏：视图组 + 数据源组 + 设置钉底）。
- 数据源切换后的用量页响应（选 Codex 时页面所有区块纯过滤收窄）。
- 设置页（侧边栏底部钉底，进入时隐藏数据源组）。

## 两部分内容与状态

**视觉部分（第 1、4 节）**：只修四类已确诊问题——ok 档黑色进度条、大读数字号失衡、层级（canvas／阴影）不显、排版细节碎。信息架构、状态语义和 [ADR-0019](../../docs/决策/ADR-0019-读数位改用定宽短码与紧凑倒计时.md) 的定宽短码保持不变。稿内标 🚩 的取值推翻了现行文档结论，**尚未定稿，不是样式真值来源**——真值仍是 `src/styles/tokens.css` 与 [设计方向与状态规范](../../docs/设计方向与状态规范.md)。逐条冲突清单与定稿门槛见 [`docs/视觉修复方向评审.md`](../../docs/视觉修复方向评审.md)。

**导航部分（第 2～3 节）**：评审**未来**主窗口导航形态，**首版不实现**：

- 左侧分组侧边栏：视图组（用量／对话／时间线）+ 数据源组（全部／Codex／Claude Code）+ 设置钉底。
- 数据源是全局状态，跨页面共享；时间范围留在各页面内。
- 设置页无数据源维度，进入时隐藏数据源组；原「返回用量」按钮由侧边栏承担。
- 🚩 主窗口 960 → 1180px（176px 侧边栏），幅度待确认。
- 对话、时间线页面内容未切片，本次只看导航形态；时间线语义未定（若为额度历史，与 [ADR-0020](../../docs/决策/ADR-0020-主窗口改为本地用量页.md)「主窗口不显示额度读数」冲突，需另议）。
- 确认后需补 ADR（导航形态）并同步 [信息架构与核心流程](../../docs/信息架构与核心流程.md) §2.3、[产品范围](../../docs/产品范围.md) 与 ADR-0020 复审条件。

额度与费用取自实机截图的真实数据，不是合成示例。

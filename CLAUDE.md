# CC Trace AI 协作规范

本文件约束参与 CC Trace 规划、设计、实现和审查的 AI。它规定**怎么工作**；产品与技术事实由 `docs/` 下的规范文档拥有。

## 1. 先读哪里

开始任何工作前，先读 [docs/README.md](docs/README.md)：它包含文档地图和**事实所有权表**，说明每个事实由哪份文档拥有。

- 要用某个事实，读它的所有者文档，不要从多份文档的相似段落拼凑结论。
- 要改某个事实，改它的所有者文档；其他文档如果重述了同一取值，改成引用。
- 要推翻某个决策，先读 [docs/决策/](docs/决策/) 里对应的 ADR。
- 不得用 Skill 的通用建议覆盖已确认的产品范围、技术边界、阶段门禁或用户决定。

### 文档的效力：默认起点，不是终点

文档记录的是**当时已知条件下的最佳判断**，不是不可推翻的结论。它的作用是省去重复推导，不是替代推导。

- **任何决策都可以推翻**，包括已确认的 ADR、阶段门禁和产品范围。发现更好的方案时必须主动提出并给出明确推荐，不得因为「文档已经这么定了」就不提、不推荐，或把它降级成备选项。
- **理由必须是第一性的。** 说某个方案不行，理由要是它本身的技术、安全、体验或成本代价。「与 ADR-XXXX 冲突」只说明需要走推翻流程，它本身不构成反对理由，不得当作论据使用。
- **推翻要显式，不要静默。** 说清楚推翻什么、为什么现在的判断和当时不同、代价是什么，取得用户同意后再改；改完补或更新 ADR，让后来者看到的是新结论，而不是互相矛盾的两份文档。
- **文档与现实不符时以现实为准**，并同步修正文档，不得为了维持文档自洽而扭曲结论。

这条不放宽「未经用户明确要求不改代码」和「最小改动」——可以随时推翻的是结论，不是流程。

最小必读集合：`docs/产品范围.md`、`docs/信息架构与核心流程.md`、`docs/状态与错误模型.md`、`docs/额度领域模型.md`、`docs/设计方向与状态规范.md`、`docs/技术架构.md`、`docs/文案与国际化.md`、`docs/日志与诊断.md`、`docs/测试策略.md`、`docs/工程与发布.md`、根目录 `PRODUCT.md` 与 `DESIGN.md`。

## 2. 通用规则

- 未经用户明确要求，不运行 build、lint、type-check、test、dev、浏览器自动化或平台构建。
- **代码自检习惯**：每次改完代码，对全部改动文件（`.ts`/`.vue` 一起）跑 esbuild 语法校验（秒级、不写产物）；改动涉及有单测的组件或逻辑时，跑对应的 vitest 文件，不跑全量。每次 `replace` 后回读改动区域，大改动用 `git diff` 自审。改动涉及 `.rs` 文件时，在 `src-tauri/` 下跑 `cargo fmt` 与 `cargo clippy --all-targets -- -D warnings` 后再提交，避免 CI 的 Rust format 与 lint 步骤失败。**不运行** `build`、`tauri dev`、全量 `type-check`/`test` 等耗时命令，除非用户明确要求，或高风险改动（跨文件契约、状态语义等）时先提议并征得同意。
- 只做任务真正需要的最小改动，不顺手重构无关代码。
- 实现值与文档值双向同步：窗口尺寸、色值、参数基线在代码里有真值，改一侧必须同时改另一侧。
- 没有验证过的结论不得写成已验证；平台相关结论必须标注平台，未验证的平台明确写未验证。
- 无法确定的取值写成“> 待确认”，并说明假设错误会影响什么。
- 首版不为后续能力预留空页面、空导航、占位卡片或不可用入口。
- Skill 导致的设计选择、风险，或与项目文档的冲突，必须在交付说明中明确记录。

## 3. Skills 使用原则

- 任务命中已安装 Skill 的适用范围时，先读该 Skill 的 `SKILL.md`，再开始设计、实现或审查。
- 每次只使用完成任务所需的最小 Skill 集合，不为形式一次性加载全部 Skills。
- 使用前说明名称和用途；Skill 缺失或不可读取时明确说明，并采用最接近的项目内规范继续。
- Skill 只能改善执行质量，不能扩大功能范围、提前跨阶段实现或引入无关重构。

## 4. 当前阶段状态

| 阶段 | 状态 |
|---|---|
| 1–4 项目身份、产品定义、首版范围、数据边界 | 已确认 |
| 5 旧版事实整理 | 已提炼为 `docs/额度领域模型.md`；Codex、Claude Code 脱敏 Fixture 已加入，真机复核待第 12 阶段 |
| 6 信息架构 | 已确认。主窗口按 [ADR-0020](docs/决策/ADR-0020-主窗口改为本地用量页.md) 改为本地用量页，不再显示额度；Timeline 与 Conversations 已作为主窗口第二、三视图进入首版，页面拓扑待后续统一导航形态决策 |
| 7 设计方向 | 已确认；视觉语言按 [ADR-0012](docs/决策/ADR-0012-视觉方向改为HeroUI风格.md) 由 Reset Rail 改为 HeroUI 风格，再按 [ADR-0017](docs/决策/ADR-0017-系统区域显示额度数字与余量分档.md) 把布局对齐 HeroUI 探索稿并新增余量四档色，再按 [ADR-0019](docs/决策/ADR-0019-读数位改用定宽短码与紧凑倒计时.md) 把读数位改成定宽短码与紧凑倒计时，再按 [ADR-0028](docs/决策/ADR-0028-全部表面改为贴合式弱化卡片层级.md) 把层级机制改为贴合式弱化卡片（删静止阴影、hairline 描边、KPI 总览行、侧边栏蓝底白字、字号 scale 收敛）；信息架构与状态语义不变 |
| 8 交互原型 | 已确认；原独立设置窗口路径已失效，同窗路径待补验 |
| 9–10 技术架构与工程基线 | 已完成：CI、lint、测试骨架与 MIT License 均已建立 |
| 11 双平台桌面壳 | 进行中：三维契约、调度、退避、设置持久化、首次启动与设置同窗已落地；本轮自动化检查已通过，之后 **macOS 实机需重跑**，Windows 实机从未跑过 |
| 12 Provider 最小闭环 | 进行中：按 [ADR-0015](docs/决策/ADR-0015-第12阶段先于桌面壳实机验证启动.md) 与第 11 阶段并行启动。2026-07-27 在 macOS 完成首次真实数据验证，两个 Provider 闭环跑通；手动刷新、缓存、失败状态与身份变化实机未验证，**Windows 侧全部未验证** |
| 13–16 首版实现、初始化、验证、验收 | 进行中：用量 Rust 数据切片、在线 Standard／Fast 价格目录、缺价与手动刷新、SQLite v2 重计价、Fast 等效 Token、脱敏 Fixture 与测试代码已落地；紧凑费用 Vue、费用行扫描状态、固定 5 分钟增量调度已补，2026-08-08 已验证通过；Pi 与 OpenCode 用量数据源已进入首版（schema v4，Rust 扫描＋Fixture＋测试，2026-08-08）；统计服务过滤（设置组＋用量页/图表/模型/对话统一过滤＋全关空态）已落地；隐私模式（紧凑入口隐藏账号，仅 UI 隐藏）已落地；主窗口已按 [ADR-0023](docs/决策/ADR-0023-视觉修复方向定稿.md)／[ADR-0024](docs/决策/ADR-0024-主窗口分组侧边栏导航.md) 定稿视觉取值并重构为分组侧边栏导航＋四源矮宽卡＋7 列模型表＋设置卡片形态，再按 [ADR-0028](docs/决策/ADR-0028-全部表面改为贴合式弱化卡片层级.md) 改为贴合式弱化卡片层级（删静止阴影、hairline 描边、KPI 总览行、侧边栏蓝底白字、字号 scale 收敛，2026-08-09，vue-tsc／eslint／prettier／vitest 通过，浏览器与实机验证未做）；官方服务状态已按 [ADR-0026](docs/决策/ADR-0026-Statuspage状态链进入首版.md) 落地（Statuspage 状态链、5 分钟独立循环、紧凑面板状态点与开关、独立契约与事件，2026-08-09 自动化通过）；真实全量索引、双平台实机与发布均未做 |
| 17 独立发布 | 基础设施已实跑：`main` 与 `v0.1.12` tag 已推送，Release workflow 于 2026-08-08 成功产出 macOS AS／Intel DMG＋ZIP 与 Windows x64 NSIS EXE 及 `SHA256SUMS.txt`（公开测试 Release，非平台实机验收）；正式公开仍依赖第 16 阶段 |

逐项进度与勾选见 [执行清单](docs/Tauri桌面端重新开发执行清单.md)。

### 门禁

- 第 6～8 阶段未经用户确认，不开始正式业务页面。
- 交互原型未经用户确认，不实现 Tray 桌面壳。
- ~~macOS／Windows 桌面壳未经实机验证，不开始 Provider 最小闭环。~~ 已由 [ADR-0015](docs/决策/ADR-0015-第12阶段先于桌面壳实机验证启动.md) 撤销：两个阶段并行推进。但第 11 阶段的实机走查项一律不得因此标记为已通过，Windows 侧任何行为不得写成已验证。
- 三维状态契约已是最终形态；接入真实 Provider 时只替换 `src-tauri/src/providers/` 内部实现，不改契约、调度、命令与前端，见 [ADR-0009](docs/决策/ADR-0009-合成数据下沉Rust并提前使用正式契约.md)。
- `docs/额度领域模型.md` 的“待复核清单”未处理，不把字段解析当作已验证。
- 未进入 `docs/产品范围.md` 的能力，不进入首版实现。
- 每完成一个阶段，同步更新执行清单勾选、本文件的阶段状态表和相关文档。

## 5. 按任务类型的规则

### 5.1 改产品范围、信息架构或核心流程

- 使用 `impeccable` 检查信息层级、窗口职责、认知负担和异常恢复路径。
- 职责、边界、验收标准或平台差异不明确时，先用 `grilling` 逐项澄清，不猜测。
- 改动必须同时检查：紧凑入口、主窗口、设置、首次启动、macOS Menu Bar 与 Windows Tray 的职责关系是否仍然自洽。
- 扩大范围的改动必须同时更新 `docs/产品范围.md` 的“首版必须／简化／后续／不做”和“首版完成标准”。

### 5.2 改视觉方向或 UI 实现

- 使用 `frontend-design` 建立或维持独立、克制、符合 CC Trace 品牌的视觉方向，避免模板化界面。
- 使用 `apple-design` 审查 macOS Menu Bar、窗口、反馈、动效和平台习惯；结论不得直接套用到 Windows，必须给出 Windows 等价交互。
- 使用 `impeccable` 覆盖视觉层级、状态表达、响应式、主题、中英文、长文本、键盘和无障碍。
- 进入间距、边框、阴影、按压反馈和 reduced motion 等细节时使用 `make-interfaces-feel-better`。
- 提交前的 UI 审查使用 `web-design-guidelines`；出现明显模板化或 AI 默认风格时再使用 `kill-ai-slop`，不把它当每次改动的固定步骤。
- 新增或改名设计 token 时同步 `docs/设计方向与状态规范.md` 第 3.4 节的映射表。
- 状态的视觉表达可以调整，状态的**语义**只能改 `docs/状态与错误模型.md`。
- 着色有两个独立维度，各只有一处实现，组件不得自己重新判断：可用性基调在 `src/lib/status.ts`，额度余量分档在 `src/lib/quotaTone.ts`。分档边界由 [ADR-0017](docs/决策/ADR-0017-系统区域显示额度数字与余量分档.md) 拥有。
- 所有用户可见字符串走 i18n，中英文同时提交；key 命名、时间与数字格式、错误文案按 `docs/文案与国际化.md`，不在组件里硬编码任何一种语言。

### 5.3 改 Rust 核心、Provider 或凭据

- 先读 `docs/额度领域模型.md` 与 `docs/状态与错误模型.md`，再读 `docs/技术架构.md` 的契约与参数基线。
- 凭据只在 Rust 层按只读、最小范围发现；不通过 command、event、前端日志或普通应用日志输出秘密。
- 除 token 刷新结果按 [ADR-0014](docs/决策/ADR-0014-token刷新结果回写外部凭据.md) 原子回写同一来源外，不写回、不修改、不删除外部凭据来源，也不写入 token、过期时刻与 Provider 自有刷新元数据之外的字段。
- 刷新、节流、退避、超时和 token 刷新提前量使用参数基线的值；需要调整时先改文档再改代码。
- 三个状态维度独立序列化；`no_credentials`、`unsupported`、`rate_limited` 不得降级为普通 `error`。
- 身份变化必须丢弃该 Provider 的快照与退避。
- 日志按 `docs/日志与诊断.md`：秘密永不落盘，持有 token 的类型必须手动屏蔽 `Debug`，不用 `#[derive(Debug)]` 直接暴露。
- 先补脱敏 Fixture 与 Rust 单元测试，再接第二个 Provider 并验证故障隔离。

### 5.4 改桌面壳或平台适配层

- 先读 `docs/桌面壳验证记录.md` 的“尚未完成”，不重复已验证项，也不把条件编译当作实机通过。
- Tray、窗口生命周期、锚点、失焦隐藏、开机启动和平台菜单留在 Rust／Tauri 平台层，不在 Vue 业务代码里散落 `isMac` / `isWindows` 判断。
- 窗口尺寸和材质按 `docs/设计方向与状态规范.md`；改尺寸必须同时改 `src-tauri/tauri.conf.json`。
- 新增任何 Tauri capability 时记录它对应的用户功能，保持最小权限。
- 桌面壳合成数据只服务验证，不得演化成第二套业务状态源。
- 系统区域显示的额度来自 `AppCore::emit_quota_state` 的同一份状态（见 [ADR-0017](docs/决策/ADR-0017-系统区域显示额度数字与余量分档.md)）；菜单栏徽标不得自己订阅事件再算一遍，那会变成第二个数据源。

### 5.5 写测试或做验收

- 范围与最低门槛以 `docs/测试策略.md` 为准。
- 状态覆盖以 `docs/状态与错误模型.md` 第 2 节的组合矩阵为准，不按单个状态值罗列。
- 验收逐项对照 `docs/产品范围.md` 的“首版完成标准”，不把后续能力悄悄带入首版。
- 实机结果写进验证记录文档，注明平台与日期。

### 5.6 配 CI、改版本或准备发布

- 范围、命令、CI 边界与版本规则以 `docs/工程与发布.md` 为准。
- CI 只做编译、类型、格式、lint 和测试；不打包、不签名、不发起真实 Provider 请求、不依赖 `../cc-bar`。
- GitHub Release workflow 与 CI 分离，只在 `v*` tag 上构建并直接公开 Release；公开 Release 不等于平台实机验收或首版正式发布。
- 改版本号时 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 三处一起改。
- 发布前逐项走 `docs/工程与发布.md` 的发布检查单；许可证由产品所有者决定，不代选。
- **不得提出任何以购买 Apple 开发者账号或代码签名证书为前提的方案**，见 [ADR-0016](docs/决策/ADR-0016-不购买Apple开发者账号.md)。macOS 开发期使用免费 Apple ID 的 Apple Development 证书稳定钥匙串 ACL；GitHub macOS 产物使用 ad-hoc 签名，不上传个人证书；Windows 产物不签名。

### 5.7 改文档本身

- 遵守 [docs/README.md](docs/README.md) 第 4 节的修改规则。
- 新增或推翻决策时写或更新 ADR，规范文档只留结论。
- 过期文档移入 `docs/archive/` 并注明冲突时以哪份为准，不直接删除。
- 需要真实页面验证时，取得用户同意后使用 `playwright` Skill 或已授权的浏览器工具。
